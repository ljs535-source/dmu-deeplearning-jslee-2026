# ---------------------------------------------------------------
#  .gitignore 배포본
#
#  ★ 파일 이름이 점으로 시작하면 Windows 탐색기에서 잘 보이지 않아
#    .txt 로 배포한다. 수업에서는 이 내용을 PowerShell 로 직접 만든다.
#
#  직접 만드는 방법 (%DL2026_HOME% 에서):
#    아래 "여기서부터" ~ "여기까지" 사이 내용을 .gitignore 로 저장하거나,
#    02_venv_commands.ps1 블록 10 을 복사해서 실행한다.
# ---------------------------------------------------------------

# ===== 여기서부터 =====

# 가상환경 — 절대 커밋하지 않는다. requirements.txt 로 재현한다.
venv/
tmpvenv/
wrongvenv/
.venv/

# 파이썬 캐시
__pycache__/
*.pyc
*.pyo

# 주피터 체크포인트
.ipynb_checkpoints/

# 환경변수·비밀키
.env

# 데이터셋 (용량이 크다)
data/
datasets/

# 학습된 모델 가중치 (용량이 크다)
*.pth
*.pt
*.ckpt
*.safetensors

# OS·에디터가 만드는 파일
Thumbs.db
desktop.ini
.DS_Store
.vscode/

# ===== 여기까지 =====
